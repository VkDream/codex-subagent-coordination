interface:
  display_name: "01·诊断"
  short_description: "Produce one complete evidence-based diagnosis package"
  default_prompt: "Use $kb-role-diagnosis to produce one complete diagnosis package for the current milestone."
policy:
  allow_implicit_invocation: true
