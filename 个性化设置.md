# 我的 Codex 个性化设置

这套设置由三部分组成：回答风格、项目工作约定和子智能体角色配置。分享的是一份经过整理的参考设置，不是可以直接覆盖到所有账号的通用配置。

| 设置 | 分享内容 | 说明 |
|---|---|---|
| 回答风格 | pragmatic | 个人偏好参考；按自己的习惯选择 |
| 自定义指令 | rules/GLOBAL_AGENTS.md | 已去除本机路径，包含子智能体协作与证据规则 |
| 子智能体 | optional-config/agents/ | 19 份角色模板，含职责、个人模型选择和派生边界 |
| 知识库 | knowledge-base/ | 从实际治理方法提炼的空白模板，不含私人知识库 |

## 个性化角色

配置按职责分为通用执行、只读探索、文档与界面、01—05 专业角色，以及日志、证据链、数据分析等专项助手。kb-01 至 kb-05 允许在父任务授权、客户端支持和明确工作边界下调用白名单助手；专项助手配置为叶节点。配置里保留的模型与推理值表示个人选择，不证明接收者环境能够加载或实际启用。

## 合并到自己的 Codex

1. 阅读全局规则，删改不适合自己的约束。
2. 按 README 中的占位符说明替换知识库、Skill 和全局指令路径。
3. 备份已有配置后再比较、合并；不要直接覆盖整个 config.toml，也不要照搬作者的私人配置文件。
4. 只放置并启用自己会用到的角色。先核实客户端所需的目录、字段、模型和权限设置。
5. 用一个小任务检查指令是否生效、角色是否加载、父子回包是否按预期工作。

配置、enabled 状态和工具列表不能证明模型、推理强度或服务层在运行时实际生效。无法确认时应如实标为 UNKNOWN。

项目专属限制应写入项目自己的规则。授权、Git、真实数据、硬件、外部发布和生产操作仍按当前任务所需的单独授权执行。