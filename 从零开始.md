# 从零开始搭建子智能体协作与个性化设置

本包提供个性化工作约定、知识库治理模板、Skill 和子智能体角色配置。复制文件不会自动安装 Skill、启用角色或创建任务。

## 1. 确认三个位置

先确认实际使用的知识库目录、Skill 目录和全局指令文件。模板里的这些标记需要手工替换，不是会自动解析的环境变量：

| 标记 | 替换内容 |
|---|---|
| {KB_ROOT} | 知识库根目录 |
| {SKILLS_ROOT} | 存放 Skill 的目录 |
| {SKILLS_ROOT_URI} | 同一 Skill 目录的绝对 file URL，注意编码空格等字符 |
| {GLOBAL_AGENTS} | 实际生效的全局 AGENTS.md 文件路径 |

替换后只检查本包副本里的引用，确认每个标记都指向自己的实际文件。

## 2. 选择全局工作约定

阅读 rules/GLOBAL_AGENTS.md，按自己的使用习惯删改或合并，再备份后更新现有全局指令。保留已有 AGENTS.override.md 等覆盖文件，先确认客户端实际读取哪个入口。

optional-config/config.example.toml 只是配置片段。先核实版本是否支持，再合并需要的字段；不要整份替换 config.toml。模型、推理、服务层、并发和 Fast 状态需要用当前运行时确认。

## 3. 使用知识库模板

将 knowledge-base 文件夹里的内容复制到自己选择的空目录，避免多套一层 knowledge-base。先读入口 AGENTS.md 和 00_Governance/RULES.md。模板不含真实项目资料；创建示例项目时应换成自己已确认的项目和路径。

知识库默认只按入口和当前项目需要读取，不要为了试用而导入全部聊天、历史或私人资料。一次内聚项目目标或里程碑保留一份主 RUN。

## 4. 安装需要的 Skill 和角色

本包的 skills 文件夹包含 codex-workflow-router 与六个 kb-role 阶段接力 Skill。按 OpenAI Skill 文档和当前客户端要求选择安装位置，确认运行环境实际识别这些 Skill。kb-role Skill 只在明确启用页面式阶段接力时加载。

optional-config/agents 中是 19 个供原生父子子智能体使用的可选 TOML 角色。根据客户端当前加载方式挑选需要的文件，合并模型和权限设置；不要直接覆盖已有角色，也不要把配置预设当成运行态证明。kb-01 至 kb-05 仅在父任务授权且原生协作工具支持时派发白名单专项助手，其他角色不派生子智能体。

## 5. 用一个清楚的小任务试用

可以让 Codex 先做一个只读检查：确认读取了哪个全局指令、知识库入口是否正确、哪些 Skill 与角色实际可用。再用一个范围明确的小任务试用父子协作，让主任务核对回包证据和实际结果。

普通分工使用原生子智能体，不需要创建额外任务页。只有你明确要求页面式阶段接力时，再按单独的接力协议启动。外部发布、Git、真实数据、硬件或生产操作按当前任务要求另行授权。